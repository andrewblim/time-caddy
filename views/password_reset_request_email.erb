Hello <%= @password_reset_user.username %>,

You or someone with your username or email address has tried to reset your password. We require email confirmation before permitting a reset, so that we can be sure it's really you. Your confirmation code is

<%= "#{@password_reset_token}\n" %>

To reset your password, please visit the following page and enter the confirmation code along with the desired new password:

<%= "#{URI.join(base_url, "/password_reset?url_token=#{@password_reset_url_token}")}\n" %>

If you didn't request to reset your password, please ignore the above and notify <%= "#{settings.support_email}.\n" %>

This password reset request will expire after a period of time.

Thanks,
time-caddy
